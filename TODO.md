# TODO

## P0 安全与可运维性

- [ ] 移除默认管理员固定账号密码初始化逻辑，改为首次部署显式创建管理员或通过环境变量注入初始管理员。
- [ ] 强制要求生产环境配置 `DYNAMICS_JWT_SECRET`、`DYNAMICS_PASSWORD_SALT`、`DYNAMICS_REFRESH_SECRET`，避免使用开发占位值启动。
- [ ] 将 refresh cookie 的 `secure=False` 改为按环境切换，生产环境默认启用 `Secure`。
- [ ] 为管理接口补充更完整的输入校验与错误响应约束，避免前后端靠隐式约定工作。
- [ ] 为登录、refresh、更新令牌使用增加基础限流，降低暴力尝试与令牌探测风险。

## P1 后端质量与正确性

- [ ] 为 `target` 解析、query 合并、slug 唯一性、令牌更新、refresh session 等核心逻辑补齐自动化测试。
- [ ] 为公开路由和管理 API 增加端到端测试，覆盖 `/s/{slug}`、`/i/{slug}`、`/api/auth/*`、`/api/update/by-token/*`。
- [ ] 将当前手写 SQL 初始化升级为可维护的迁移机制，避免后续 schema 演进困难。
- [ ] 补充统一的异常处理中间件与结构化日志，方便排查线上问题。
- [ ] 为 Repo 层和服务层补齐更清晰的边界约束，减少路由层直接拼装响应的重复代码。

## P1 客户端接入

- [ ] 在 `client/` 中真正实现“获取当前 IP 后调用 `/api/update/by-token/<token>`”的上报逻辑。
- [ ] 为客户端增加 `dynamic_ip` 与 `dynamic_url` 两种上报模式配置，而不只停留在文档说明。
- [ ] 为客户端的上报失败、令牌失效、目标模式不匹配等场景补充可见错误提示与重试策略。

## P2 前端体验与可维护性

- [ ] 将当前单文件 `frontend/src/App.vue` 拆分为页面、表单、表格和状态管理模块，降低维护成本。
- [ ] 增加前端路由、页面级状态划分与更明确的加载/空状态/错误状态反馈。
- [ ] 为 target、slug、token、logs 增加搜索、筛选与分页能力。
- [ ] 改进移动端布局与表格展示，避免内容较多时管理台可用性下降。

## P2 产品能力增强

- [ ] 支持用户修改密码与管理员禁用用户后的会话失效处理。
- [ ] 增加 target 健康检查与失效可见性，而不仅仅记录更新日志。
- [ ] 细化 `/i/{slug}` 的公开字段策略，区分完整 URL 暴露与受限信息投影。
- [ ] 增加更正式的部署说明，包括同域部署、反向代理、静态资源托管和环境变量示例。

## P3 持久化与扩展路线

- [ ] 为 `SQLite -> PostgreSQL` 迁移提前定义兼容的数据访问契约和迁移步骤。
- [ ] 评估是否需要将 refresh session、更新日志和审计能力进一步抽象，支撑多实例部署。
- [ ] 在有真实使用量后评估是否需要引入速率限制存储、缓存层或异步任务机制。
