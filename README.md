
# DyNamicS

用于在苏州大学校园网环境内部署私有DNS服务器并动态上报校园网ip实现固定域名访问自建服务的一套设施。

## 现有设施

1. 一个脚本用于自动登录且获取校园网IP地址，并将该信息发送到指定的URL进行上报。

## 未完成设施

1. DNS后端，用于将现有数据动态生成配置文件更新DNS软件状态。
2. DNS配置前端，使用户能够配置自己的域名解析服务。

## 使用方法

详见对应文件夹内的README文件。

- IP上报以及自动登录: `client`
- DNS后端: `backend`
- DNS配置前端: `frontend`
